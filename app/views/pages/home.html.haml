.two_column
  - if user_signed_in?
    = paginate @users
    %ul.users
      - @users.each do |user|
        %li
          = link_to user.name, user
    = paginate @users
  - else
    %h3 Have someone you'd like to thank?
    = render 'shared/short_intro'
    = render 'shared/thankyou_steps'
    = render 'shared/thankyou_tips'

