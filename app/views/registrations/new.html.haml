%h3
  Want to sign up the Merciboq! way?  Just write a thankyou email.
= render 'shared/thankyou_steps'
%h3
  Want to sign up the traditional way?
= form_for(resource, :as => resource_name, :url => registration_path(resource_name)) do |f|
  = f.email_field :email
  = f.submit raw "Sign up"
= render :partial => "devise/shared/links"

