- if user_signed_in?
  %li.divider-vertical
  %li
    = link_to "Logout", destroy_user_session_url(:host => request.domain)
- else
  %li.divider-vertical
  %li
    = link_to('Login', login_url(:host => request.domain))

