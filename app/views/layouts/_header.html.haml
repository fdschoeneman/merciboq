%header
  .logo
    = link_to "MerciboQ!", root_path
  %nav
    %ul
      - if signed_in?
        %li= link_to_unless_current "Thankyous", thankyou_path
        |
        %li= link_to_unless_current "Welcomes", welcome_path
      -else
        %li= link_to_unless_current "Home", root_path
        |
        %li= link_to_unless_current "About", about_path(:subdomain => false)
        |
        %li= link_to_unless_current "Help", help_path
      |
      = render 'devise/menu/registration_items'
      |
      = render 'devise/menu/login_items'
  %br

