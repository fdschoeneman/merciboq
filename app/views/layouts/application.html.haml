!!!
%html
  %head
    %title= title
    %link{:href => "http://fonts.googleapis.com/css?family=Pompiere|Montez|Waiting+for+the+Sunrise|La+Belle+Aurore|Yellowtail|Calligraffitti|Covered+By+Your+Grace", :rel => "stylesheet", :type => "text/css"}/
    = stylesheet_link_tag     :application
    = javascript_include_tag  :application
    // = javascript_include_tag  :defaults
    = csrf_meta_tags
  %body
    .container
      = render 'layouts/header'
      %br
      - flash.each do |name, msg|
        = content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String)
      = yield
      = render 'layouts/footer'
      = debug(params) if Rails.env.development?
      = render 'layouts/googleanalytics'

